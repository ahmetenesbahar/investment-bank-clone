import React from "react";
import { HeaderContainer, HeaderText } from "./Header.styles";

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <HeaderText>Son İşlemlerim</HeaderText>
    </HeaderContainer>
  );
};

export default Header;
