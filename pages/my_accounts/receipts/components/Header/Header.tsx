import React from "react";
import { HeaderContainer, HeaderText } from "./Header.styles";

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <HeaderText>Dekontlarım</HeaderText>
    </HeaderContainer>
  );
};

export default Header;
